import { AppState } from '../index';

// export const getCardList = (state: AppState) => state.card.cardList ? state.card.cardList : [];
//
// export const getTrelloListArray = (state: AppState) =>
//   state.trelloList.trelloListArray ? state.trelloList.trelloListArray : [];
